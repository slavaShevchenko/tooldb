import { appSeo } from './app'

export const toolsSeo = {
  title: `Tools${appSeo.titleSeparator}${appSeo.siteName}`,

  description:
    'Browse the complete collection of digital tools.',
} as const