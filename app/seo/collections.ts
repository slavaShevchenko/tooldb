import { appSeo } from './app'

export const collectionsSeo = {
  title: `Collections${appSeo.titleSeparator}${appSeo.siteName}`,

  description:
    'Discover curated collections of digital tools.',
} as const