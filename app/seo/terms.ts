import { appSeo } from './app'

export const termsSeo = {
  title: `Terms of Service${appSeo.titleSeparator}${appSeo.siteName}`,

  description:
    'Read the terms that apply when you access and use ToolDB.',
} as const
