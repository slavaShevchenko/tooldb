import { appSeo } from './app'

export const alternativesSeo = {
  title: `Alternatives${appSeo.titleSeparator}${appSeo.siteName}`,

  description:
    'Compare top-rated alternatives to popular software tools. Find better pric',
} as const